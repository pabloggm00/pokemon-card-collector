# ========================================
# Pokemon Card Collector - Environment Variables
# ========================================
# 
# Este archivo configura las variables de entorno para Docker Compose.
# Se usa SOLO cuando ejecutas el proyecto con Docker (docker-compose up).
#
# INSTRUCCIONES:
# 1. Copia este archivo y renómbralo a .env
# 2. Ajusta los valores según tus necesidades
# 3. NO subas el archivo .env a GitHub (ya está en .gitignore)
#
# NOTA: Para desarrollo local (sin Docker), usa backend/.env.example
# ========================================

# ===== DATABASE CONFIGURATION (DOCKER) =====
# Usuario de PostgreSQL
DB_USER=pokemon_user

# Contraseña de PostgreSQL (CAMBIA ESTO EN PRODUCCIÓN)
DB_PASSWORD=pokemon_password

# Nombre de la base de datos
DB_NAME=pokemon_collector

# ===== BACKEND CONFIGURATION =====
# Puerto interno del backend (NO cambiar a menos que sepas lo que haces)
PORT=3001

# Entorno de Node.js
NODE_ENV=production

# ===== NOTAS IMPORTANTES =====
# - DATABASE_URL se genera automáticamente en docker-compose.yml desde las variables DB_*
# - El backend en Docker usa: postgresql://DB_USER:DB_PASSWORD@db:5432/DB_NAME
# - El hostname "db" solo existe dentro de la red de Docker
# - El puerto del frontend (8080) se configura en docker-compose.yml
# - PostgreSQL está expuesto en localhost:5432 para desarrollo local

