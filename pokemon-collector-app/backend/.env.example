# ========================================
# Backend - Environment Variables (DESARROLLO LOCAL)
# ========================================
# 
# Este archivo es SOLO para desarrollo local.
# NO se usa cuando ejecutas todo con Docker.
#
# INSTRUCCIONES PARA DESARROLLO LOCAL:
# 1. Copia este archivo y renómbralo a .env
# 2. Levanta solo la base de datos: docker-compose up -d db
# 3. Ejecuta el backend localmente: npm start
# 4. Ejecuta migraciones: npx prisma migrate dev
#
# IMPORTANTE: El backend en Docker usa las variables de
# docker-compose.yml, NO este archivo .env
# ========================================

# ===== DATABASE CONFIGURATION (DESARROLLO LOCAL) =====
# URL de conexión a PostgreSQL corriendo en Docker
# Apunta a "localhost" porque accedes desde fuera de Docker
DATABASE_URL=postgresql://pokemon_user:pokemon_password@localhost:5432/pokemon_collector

# ===== BACKEND CONFIGURATION =====
# Puerto del backend
PORT=3001

# Entorno de Node.js
NODE_ENV=development

# ===== NOTAS IMPORTANTES =====
# - Esta configuración es para correr el backend FUERA de Docker
# - PostgreSQL debe estar corriendo en Docker: docker-compose up -d db
# - El puerto 5432 debe estar expuesto en docker-compose.yml
# - Para producción/Docker, usa el .env de la raíz del proyecto
